Say it this way... I needed a remote controller for my Nikon D3200 camera, but 
cost and delivery time where just unrealistic...
so I quickly implemented it into an Arduino, with an IR LED.

Trigger via a push button or a command via Serial interface

